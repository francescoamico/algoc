typedef struct complex *Complex;

Complex init(double,double);
Complex add(Complex,Complex);
Complex sub(Complex,Complex);
Complex mul(Complex,Complex);
Complex divid(Complex,Complex);
void show(Complex);
